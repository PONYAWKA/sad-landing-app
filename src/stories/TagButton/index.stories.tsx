import { TagButton } from "sad-landing-lib";

export default {
  title: "TagButton",
  component: TagButton,
};

const Template = () => <TagButton text="Anime" />;

export const Default = Template.bind({});
