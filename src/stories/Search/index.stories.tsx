import { Search } from "sad-landing-lib";

export default {
  title: "Search",
  component: Search,
};

const Template = () => <Search buttonText="Find" />;

export const Default = Template.bind({});
